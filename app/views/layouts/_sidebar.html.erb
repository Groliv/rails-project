<aside>
	<div class="widget-cat" style="background-color:white">
		<div class="title_widget">Categories</div>
		 <div id="submenu">
			  <ul >
			  <% Category.roots.each do |category| %>
    			<li ><%= link_to category.name, category %>
    				<%= render './categories/submenu_cats', categories: category.children if category.children.present? %>
				</li>		
			<% end -%>
			</ul>
		</div>
	</div>

	<div class="widget-search">
		<div class="title_widget">The search</div>
	</div>

	<div class="widget-infos">
		<div class="title_widget">The my-info</div>
	</div>

	<div class="widget">
	</div>

</aside>