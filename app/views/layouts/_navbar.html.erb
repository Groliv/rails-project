<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">The Rails Braderie</a>
    </div>
    <div>
      <ul class="nav navbar-nav">
        <li><%= link_to "products", products_path, :method => :get %></li>
        <li><%= link_to "users", users_path, :method => :get %></li> 
        <% if user_signed_in? %>
        <li><a href="#">Yours products</a></li> 
        <% end %>
      </ul>
        <ul class="nav navbar-nav navbar-right"> 
        <% if user_signed_in? %>
          <li><%= link_to '<span class="glyphicon glyphicon-log-out white"></span> Sign Out'.html_safe, destroy_user_session_path, :method => :delete, class: 'btn btn-default' %></li>
        <% else %>
          <li><%= link_to '<span class="glyphicon glyphicon-log-in"></span> Sign In'.html_safe, new_user_session_path, :method => :get , class: 'btn btn-default' %></li>
          <li><%= link_to '<span class="glyphicon glyphicon-user"></span> Sign up'.html_safe, new_user_registration_path, :method => :get , class: 'btn btn-default' %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
